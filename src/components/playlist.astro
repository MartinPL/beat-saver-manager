---
import PlaylistItem from "../components/playlist-item.astro"
---

<div class="flex flex-col rounded-lg border border-gray-700 md:col-span-3 mt-6">
    <div class="py-2 px-5">
        <div class="min-w-full overflow-x-auto rounded">
            <table class="min-w-full align-middle text-sm">
                <tbody>
                    <template x-for="(map, index) in maps">
                        <PlaylistItem />
                    </template>
                </tbody>
            </table>
        </div>
    </div>
</div>
